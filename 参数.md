# YiCLI Harness 参数手册（面试向）

> 所有数值以代码实际行为为准（优先级：代码 > `AGENTS.md` > `README.md`）。
> 本文档基于 `src/main/java` 源码核对，标注了每个模块对应的源文件，便于面试时溯源。

---

## 0. 高频考点速览

| 考点 | 数值 |
|------|------|
| 各模型上下文窗口 | GLM 200k / DeepSeek 1M / Agnes 1M / Step 256k / Kimi 256k / FreeLLMAPI 128k / 讯飞星辰 128k |
| 压缩触发阈值 | `window - min(20k, window/4) - min(13k, window/8)`；200k → 约 167k，1M → 约 967k |
| ReAct 三层兜底 | token 预算（默认不限）/ 连续 3 次相同工具调用 / 50 轮硬上限 |
| 工具并行 | 最多 4 个并发，结果保持原始顺序 |
| 命令执行 | 默认 60s 超时、输出上限 8KB；整批工具 90s 超时 |
| LLM 超时 | connect 60s / read 300s / write 60s / call 600s |
| LLM 重试 | 最多 3 次（1~6）、基础退避 2s 指数增长、上限 30s、尊重 `Retry-After`、流开始后不重试 |
| MCP | 首屏等待 8s、initialize 超时 60s、自动重启默认开、并行启动最多 8 个 server |
| HITL | "全部放行"额度 10 次、审批超时默认不限（配置 >0 才生效） |
| Side-Git 快照 | 默认开启、最多保留 50 个 |
| 输入历史 | 内存 2000 条 / 文件 10000 行 |
| 后台任务 worker | 默认 2 |
| 上下文模型 | 所有参数都由 `maxContextWindow` 派生，没有"长/中/短"分档 |

---

## 1. 产品基线

| 项 | 值 |
|----|----|
| 项目名 | YiCLI（由 PaiCLI 更名而来，包名统一 `com.yicli`，环境变量统一 `YICLI_*`） |
| Banner 版本 | `v16.1.0`（与 Maven 产物版本不一致是正常状态） |
| Maven 产物 | `yicli-1.0-SNAPSHOT.jar`（Java 17+，shade 打包） |
| 执行路径 | ReAct（默认）/ Plan-and-Execute（`/plan`）/ Multi-Agent（`/team`） |
| 内置工具 | 17 个 + MCP 动态工具 `mcp__{server}__{tool}` |
| 渲染器 | `inline`（默认流式 TUI）/ `lanterna`（全屏 TUI）/ `plain`（纯文本兜底） |

---

## 2. 模型与 Provider 参数

来源：`llm/*.java`、`LlmClientFactory.java`。

| Provider | 默认模型 | 上下文窗口 | 默认 Base URL | prompt cache 模式 | 备注 |
|----------|---------|-----------|--------------|-------------------|------|
| GLM | `glm-5.1` | 200k | 动态选择（编码/多模态 API） | `glm-prompt-cache` | `glm-5v-turbo` 支持图片输入（配合 Ctrl+V / `@image:`） |
| DeepSeek | `deepseek-v4-flash` | 1M | `https://api.deepseek.com` | `automatic-prefix-cache` | 按文本 provider 处理：不支持图片，历史中的图片块替换为文本提示 |
| StepFun | `step-3.5-flash` | 256k | `https://api.stepfun.com/v1` | `step-prefix-cache` | 自动带 `reasoning_format=deepseek-style` 拿 reasoning_content |
| Kimi / Moonshot | `kimi-k2.6` | 256k | `https://api.moonshot.ai/v1` | `moonshot-context-cache` | 兼容 `MOONSHOT_API_KEY` 等 |
| Agnes | `agnes-2.0-flash` | 1M | `https://apihub.agnes-ai.com/v1` | — | OpenAI-compatible，支持流式与 tools |
| FreeLLMAPI | `auto` | 128k | `http://localhost:5173/v1` | — | 本地网关，模型可填 `/v1/models` 的 model id |
| 讯飞星辰 MaaS | 代码默认 `Qwen3.6-35B-A3B` | 128k | `https://maas-api.cn-huabei-1.xf-yun.com/v2` | — | model 必须用服务管控页的 modelId；微调用 `/config provider xfyun --lora-id <resourceId>`，以 `lora_id` header 发出；纯对话请求，不发送工具列表 |
| LlmClient 兜底 | — | 128k | — | — | 接口默认值 |

其他行为参数：

- 运行时切换：`/model <provider>`；配置持久化：`/config provider <name> ...`。
- DeepSeek / Kimi thinking 模式下，assistant tool-call 消息的 `reasoning_content` 必须随下一轮请求带回（其他 provider 只写日志/展示）。
- DeepSeek SSE 调用默认强制 HTTP/1.1（`DeepSeekClient` 专用 OkHttp client），避免部分网络下 HTTP/2 长流被远端重置。
- 检测到 `STEP_API_KEY` 时会自动内置 `step_search` 远程 MCP（显式同名配置优先），当前模型为 `step-3.7-flash*` 且就绪时，内置 `web_search`/`web_fetch` 优先转调 StepSearch。

---

## 3. 上下文与压缩参数（ContextProfile）

来源：`context/ContextProfile.java`、`memory/ConversationHistoryCompactor.java`、`agent/Agent.java`。

### 3.1 设计原则

没有"长/短/平衡"模式分档。所有参数都是 `maxContextWindow` 的简单函数，全模型走同一套行为，只是 window 大小不同导致触发时机和容量不同。

### 3.2 压缩触发阈值公式

```text
summaryReserve = min(20_000, max(1_000, window / 4))   // 给摘要输出预留
buffer         = min(13_000, max(1_000, window / 8))   // 自动压缩安全缓冲
trigger        = window - summaryReserve - buffer      // 最终 clamp 到 [1000, window-1]
compressionTriggerRatio = max(0.50, min(0.99, trigger / window))
```

各窗口触发值：

| window | summaryReserve | buffer | 压缩触发阈值 | 占比 |
|--------|---------------|--------|-------------|------|
| 128k | 20k | 13k | 95,000 | 74.2% |
| 200k | 20k | 13k | 167,000 | 83.5% |
| 256k | 20k | 13k | 223,000 | 87.1% |
| 1M | 20k | 13k | 967,000 | 96.7% |

### 3.3 派生预算

| 参数 | 公式 | 128k | 200k | 256k | 1M |
|------|------|------|------|------|----|
| agentTokenBudget（软提示） | `max(4000, window × 0.8)` | 102,400 | 160,000 | 204,800 | 800,000 |
| shortTermMemoryBudget | `max(4000, window × 0.45)` | 57,600 | 90,000 | 115,200 | 450,000 |
| memoryContextTokens（注入上限） | `max(500, min(5000, window / 200))` | 640 | 1,000 | 1,280 | 5,000（封顶） |
| MCP resource 索引 | `window ≥ 32k` 才启用 | on | on | on | on |

常量：`MIN_WINDOW = 8_000`；`MIN_COMPRESSION_TRIGGER_RATIO = 0.50`。

### 3.4 压缩行为

- 自动压缩（`compactIfNeeded`）：估算 history token ≥ trigger 才压缩；**保留最近 3 个 user 轮次**（`DEFAULT_RETAIN_RECENT_ROUNDS = 3`），只压缩更早的部分，并按 user 消息边界切割。
- 手动压缩（`/compact`，`compactNow`）：跳过阈值判断，**保留最近 1 个 user 轮次**。
- 压缩由 LLM 生成摘要（专门的 summary prompt），摘要调用失败或返回空则跳过本次压缩，不阻塞主流程。
- 两道压缩不要混淆：shortTermMemory 压缩 vs conversationHistory 压缩（后者才是防窗口超限的关键）。

---

## 4. ReAct / Agent 预算（AgentBudget）

来源：`agent/AgentBudget.java`。

| 参数 | 默认值 | 配置入口 | 说明 |
|------|--------|---------|------|
| token 预算 | `Integer.MAX_VALUE`（实质不限） | `-Dyicli.react.token.budget=N` | 累计 input+output 超过即强制收尾；长上下文 + 套餐用户默认不设硬墙 |
| 停滞窗口 | 3 | `-Dyicli.react.stagnation.window` | 连续 N 次相同工具调用判定停滞 |
| 硬轮数上限 | 50 | `-Dyicli.react.hard.max.iterations` | 兜底防御，防止死循环 |

设计取舍：`ContextProfile` 里的 agentTokenBudget（80% window）只作为 `/context` 与 token 统计的"软提示"，真正的硬限默认走 `Integer.MAX_VALUE`；CI / 自动化场景用 `-Dyicli.react.token.budget` 显式开启。

---

## 5. LLM 调用与重试参数

来源：`llm/AbstractOpenAiCompatibleClient.java`。

### 5.1 HTTP 超时

| 项 | 默认值 | 配置属性 |
|----|--------|---------|
| connect | 60s | `yicli.llm.connect.timeout.seconds` |
| read（两次 read 间隔） | 300s | `yicli.llm.read.timeout.seconds` |
| write | 60s | `yicli.llm.write.timeout.seconds` |
| call（整体兜底） | 600s | `yicli.llm.call.timeout.seconds` |

> 注意：SSE 流式下 readTimeout 是"两次 read 之间最大间隔"而非请求总时长；GLM-5.1 生成大段 reasoning 时服务端可能长时间静默，所以放宽到 300s。

### 5.2 重试

| 参数 | 默认值 | 范围 | 配置 |
|------|--------|------|------|
| 最大重试次数 | 3 | 1~6 | `yicli.llm.retry.max.attempts` / `YICLI_LLM_RETRY_MAX_ATTEMPTS` |
| 基础退避 | 2s | 0~60s | `yicli.llm.retry.backoff.seconds` / `YICLI_LLM_RETRY_BACKOFF_SECONDS` |

- 退避策略：`min(2s << min(attempt-1, 4), 30s)`，即指数增长、封顶 30s。
- 尊重服务端 `Retry-After`（取 max(计算退避, Retry-After)）。
- 可重试状态码：`408 / 429 / 500 / 502 / 503 / 504`；其余错误不重试。
- **流一旦开始输出就不再重试**（避免重复内容）；重试会回调 `onRetry` 提示。

### 5.3 Prompt Caching

| Provider | 模式 |
|----------|------|
| GLM | `glm-prompt-cache` |
| DeepSeek | `automatic-prefix-cache` |
| Step | `step-prefix-cache` |
| Kimi | `moonshot-context-cache` |

`ContextProfile` 会声明能力并把 `cached` usage 计入 token 统计与成本计算。

---

## 6. 工具执行参数（ToolRegistry）

来源：`tool/ToolRegistry.java`。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 并行工具数 | `MAX_PARALLEL_TOOLS = 4` | 共享固定线程池，结果保持原始顺序 |
| 命令超时 | 60s | `execute_command`，超时取消 |
| 工具批次超时 | 90s | 一批工具整体超时 |
| 命令输出上限 | 8,000 字符 | 截断 |
| read_file | 默认 200 行 / 上限 2,000 行 | `limit` 参数 |
| grep_code max_results | 默认 50 / 上限 200 | |
| grep_code head_limit | 默认 20 | |
| grep_code context_lines | 上限 5 | |
| grep_code max_chars | 默认 24,000 / 上限 60,000 | 结果字符预算，超出返回 `partial` + `suggested_reads` |
| web_fetch max_chars | 默认 8,000 | |
| write_file | 5MB 上限 | |
| task 子代理 | 嵌套最多 2 层 | `task` 工具分发独立探索 |

沙箱（可选）：`YICLI_SANDBOX_MODE=docker`（默认 `off`）、镜像 `YICLI_SANDBOX_IMAGE=eclipse-temurin:17-jdk`；Docker 不可用时自动回退本地执行。

---

## 7. Web 与搜索参数

来源：`web/WebFetcher.java`、`web/NetworkPolicy.java`、`web/SearchProviderFactory.java`、`web/ZhipuSearchProvider.java`。

### 7.1 web_fetch

| 项 | 值 |
|----|----|
| 整体超时 | 30s（connect 10s） |
| 响应体上限 | 5MB（流式读，超限截断） |
| 限流 | token bucket，每 60s 最多 30 次 |
| 安全策略 | 屏蔽 `file://`、内网、loopback |

### 7.2 搜索 provider 自动选择（未显式 `SEARCH_PROVIDER`）

1. 有 `GLM_API_KEY` → `zhipu`（与 GLM 推理共用 Key，零额外配置）
2. 有 `SERPAPI_KEY` → `serpapi`
3. 有 `SEARXNG_URL` → `searxng`
4. 都没有 → 占位 zhipu，`isReady()=false`

智谱引擎（`ZHIPU_SEARCH_ENGINE`）：`search_std`（0.01 元/次，默认）/ `search_pro`（0.03 元/次）/ `search_pro_sogou` / `search_pro_quark`（0.05 元/次）。

---

## 8. MCP 参数

来源：`cli/Main.java`、`mcp/McpClient.java`、`mcp/McpServerManager.java`。

| 参数 | 默认值 | 配置入口 |
|------|--------|---------|
| CLI 首屏等待 MCP 启动 | 8s | `YICLI_MCP_STARTUP_WAIT_SECONDS` / `-Dyicli.mcp.startup.wait.seconds`；超时后保留 `STARTING` 后台继续初始化 |
| initialize 超时 | 60s | `YICLI_MCP_INITIALIZE_TIMEOUT_SECONDS` / `-Dyicli.mcp.initialize.timeout.seconds` |
| server 崩溃自动重启 | true | `YICLI_MCP_AUTO_RESTART` |
| 并行启动 server | 最多 8 个 | `min(targets.size(), 8)` |
| 配置合并 | 用户级 `~/.yicli/mcp.json` + 项目级 `.yicli/mcp.json`（按 server 名覆盖） | 支持 `${PROJECT_DIR}`、`${HOME}`、`${VAR}` 展开 |
| 默认 server | `chrome-devtools`（isolated 模式，`~/.yicli/mcp.json` 不存在时自动创建） | 改 `--autoConnect` 可复用登录态 |

---

## 9. 安全与 HITL 参数

来源：`hitl/*.java`、`policy/*.java`、`config/YiCliEnv.java`。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 拦截顺序 | `HitlToolRegistry → ToolRegistry → PathGuard/CommandGuard` | 用户无法批准策略拒绝的请求 |
| HITL 全部放行额度 | 10 次 | `YICLI_HITL_APPROVE_ALL_LIMIT`；用尽后需重新审批 |
| HITL 审批超时 | 0 = 不限时 | `YICLI_HITL_TIMEOUT_SECONDS`；>0 时超时自动拒绝 |
| PathGuard | 路径强制限定在项目根内 | 绝对路径外逃 / `..` 穿越 / 符号链接逃逸均拦截 |
| CommandGuard | 辅助黑名单 | 不是主防线 |
| 微信 iLink 通道 | 非交互默认拒绝 | `execute_command` 精确白名单、`mcp__*` 白名单、`revert_turn` / 浏览器会话切换默认拒绝、文件写入由 PathGuard 限定 |
| Headless（Runtime API / 后台任务） | 非交互默认拒绝 | `write_file` 拒绝敏感文件（`.env` / `.git/` / `*.pem` / `id_rsa` / 云凭据）与密钥内容；`execute_command` 需 `YICLI_HEADLESS_COMMAND_ALLOWLIST`，MCP 需 `YICLI_HEADLESS_MCP_ALLOWLIST` |
| 权限记忆 | `~/.yicli/permissions.json` | 按 tool + 归一化参数精确匹配；`/permission` 查看/清空 |
| 审计 | `~/.yicli/audit/audit-YYYY-MM-DD.jsonl` | 危险工具按天写一行；write_file 脱敏 content、MCP 参数按字段名掩码 |
| 审批框安全 | ANSI/控制字符清洗 | 防终端注入 |

---

## 10. 持久化参数

| 项 | 默认值 | 配置入口 / 位置 |
|----|--------|----------------|
| Side-Git 快照 | 开 | `YICLI_SNAPSHOT_ENABLED=true`；`YICLI_SNAPSHOT_MAX=50`；排除 `.git,.yicli/snapshots,target,node_modules,dist,.idea,*.class,*.jar`；目录 `~/.yicli/snapshots/<project_hash>/<worktree_hash>/.git` |
| 会话 | 退出自动保存 | `~/.yicli/sessions/session_*.jsonl`；`/sessions` 列表、`/resume <id>` 恢复；取消/失败轮次自动留检查点 |
| 输入历史 | 2000 条 / 文件 10000 行 | `-Dyicli.history.size` / `-Dyicli.history.fileSize`（或 `YICLI_HISTORY_SIZE` / `YICLI_HISTORY_FILE_SIZE`）；文件 `~/.yicli/history/input.history`；忽略空白、重复、密钥、base64 图片、超长输入 |
| 长期记忆 | 项目级作用域 | `~/.yicli/memory`；`/save`（`--global` 跨项目）；`/memory list / search / delete / clear` |
| 遥测 | 开 | `YICLI_TELEMETRY_ENABLED=true`；`~/.yicli/telemetry/YYYY-MM-DD.jsonl`；成本率可配 `yicli.telemetry.cost.input/output.per.million` |

---

## 11. Runtime API 与后台任务

来源：`runtime/api/RuntimeApiServer.java`、`runtime/task/DurableTaskManager.java`、`cli/Main.java`。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| Runtime API 端口 | 8080 | `serve --http --port <port>` |
| 监听地址 | 仅 127.0.0.1 | |
| 鉴权 | 强制 API Key | `YICLI_RUNTIME_API_KEY`；`Authorization: Bearer ...` 或 `X-YiCLI-API-Key: ...` |
| 后台任务队列 | SQLite | `~/.yicli/tasks/tasks.db`（`YICLI_TASK_DIR`） |
| worker 数 | 2 | `YICLI_TASK_WORKERS` / `-Dyicli.task.workers` |

---

## 12. 日志参数

来源：`cli/Main.java`、`config/YiCliEnv.java`、`.env.example`。

| 参数 | 默认值 |
|------|--------|
| 级别 | `INFO`（`YICLI_LOG_LEVEL`） |
| 目录 | `~/.yicli/logs`（`YICLI_LOG_DIR`） |
| 单文件滚动体积 | `10MB`（`YICLI_LOG_MAX_FILE_SIZE`） |
| 保留天数 | `7`（`YICLI_LOG_MAX_HISTORY`） |
| 归档总容量 | `100MB`（`YICLI_LOG_TOTAL_SIZE_CAP`，超出自动清理更早日志） |

---

## 13. RAG 与 Embedding 参数

来源：`rag/*.java`。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| Embedding provider | `ollama` | `EMBEDDING_PROVIDER`（ollama / openai / zhipu / glm） |
| Embedding 模型 | `nomic-embed-text:latest` | `EMBEDDING_MODEL` |
| Embedding 地址 | `http://localhost:11434` | `EMBEDDING_BASE_URL` |
| 代码块大小 | `MAX_CHUNK_CHARS = 2000` | 中文约 1 字符 = 2~3 token，安全适配小上下文 |
| search_code topK | 默认 5 / 上限 30 | `top_k` 参数 |
| hybrid 检索 | 先取 `max(topK*2, 10)`，再按文件去重（每文件最多 2 条） | `CodeRetriever` |
| 语义检索注入预算 | `window / 200`，封顶 5000 | 即 memoryContextTokens |

---

## 14. Skill / Prompt / 渲染参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| Skill system prompt 索引 | 最多 20 个 / 4KB | `MAX_ENABLED_SKILLS = 20`、`MAX_INDEX_BYTES = 4096`（`SkillIndexFormatter`） |
| Skill 加载 | `load_skill` → `SkillContextBuffer` | 下一轮 user message 前置注入 |
| Skill 位置 | jar 内置 `resources/skills/` → 用户级 `~/.yicli/skills/` → 项目级 `.yicli/skills/` | 后者覆盖前者；内置 skill 自动解压到 `~/.yicli/skills-cache/` |
| PAI.md 注入 | 用户级 `~/.yicli/PAI.md` → 项目根 `PAI.md` → `.yicli/PAI.md` → `PAI.local.md` → `.yicli/PAI.local.md` | 总注入有字符预算 |
| 渲染器 | `inline` | `YICLI_RENDERER=inline\|lanterna\|plain`；`YICLI_NO_STATUSBAR=true` 禁用底部 dock |
| 输入提示符 | `* `，右提示 `message / @path / @image` | JLine 4 LineReader |
| 代码块折叠 | Ctrl+O | 折叠头 `⏵ code: <lang> · N 行` |
| 思考面板 | 保留最近 4096 字符 / 最多 4 行 | `MAX_REASONING_CHARS` / `MAX_REASONING_ROWS` |
| 首屏 | YiCLI 块状字标 + 模型/MCP/Skill/ReAct 状态 + 3 条 tips | Banner 版本 `v16.1.0` |

---

## 15. 配置入口速查（YiCliEnv 注册表）

统一解析顺序：**系统属性 > 环境变量 > 默认值**（`config/YiCliEnv.java`）。

| 环境变量 | 系统属性 | 默认值 | 说明 |
|----------|---------|--------|------|
| `YICLI_RENDERER` | `yicli.renderer` | `inline` | 渲染器形态 |
| `YICLI_LOG_LEVEL` | `yicli.log.level` | `INFO` | 日志级别 |
| `YICLI_LOG_DIR` | `yicli.log.dir` | `~/.yicli/logs` | 日志目录 |
| `YICLI_LSP_ENABLED` | `yicli.lsp.enabled` | `true` | write_file 后 LSP 诊断 |
| `YICLI_LSP_MAX_DIAGNOSTICS` | `yicli.lsp.max.diagnostics` | `20` | 单次诊断注入上限 |
| `YICLI_SNAPSHOT_ENABLED` | `yicli.snapshot.enabled` | `true` | Side-Git 快照开关 |
| `YICLI_SNAPSHOT_MAX` | `yicli.snapshot.max` | `50` | 快照上限 |
| `YICLI_MCP_STARTUP_WAIT_SECONDS` | `yicli.mcp.startup.wait.seconds` | `8` | 首屏等待 MCP |
| `YICLI_MCP_INITIALIZE_TIMEOUT_SECONDS` | `yicli.mcp.initialize.timeout.seconds` | `60` | MCP initialize 超时 |
| `YICLI_MCP_AUTO_RESTART` | `yicli.mcp.auto.restart` | `true` | MCP 崩溃自动重启 |
| `YICLI_LLM_RETRY_MAX_ATTEMPTS` | `yicli.llm.retry.max.attempts` | `3` | LLM 重试次数（1~6） |
| `YICLI_LLM_RETRY_BACKOFF_SECONDS` | `yicli.llm.retry.backoff.seconds` | `2` | 重试基础退避 |
| `YICLI_NO_STATUSBAR` | `yicli.no.statusbar` | `false` | 禁用底部 dock |
| `YICLI_SANDBOX_MODE` | `yicli.sandbox.mode` | `off` | 命令沙箱（docker） |
| `YICLI_SANDBOX_IMAGE` | `yicli.sandbox.image` | `eclipse-temurin:17-jdk` | 沙箱镜像 |
| `YICLI_TELEMETRY_ENABLED` | `yicli.telemetry.enabled` | `true` | 遥测落盘 |
| `YICLI_HITL_TIMEOUT_SECONDS` | `yicli.hitl.timeout.seconds` | `0` | 审批超时（0=不限） |
| `YICLI_HITL_APPROVE_ALL_LIMIT` | `yicli.hitl.approve.all.limit` | `10` | 全部放行额度 |
| `YICLI_HEADLESS_COMMAND_ALLOWLIST` | `yicli.headless.command.allowlist` | 空 | 无头模式命令白名单 |
| `YICLI_HEADLESS_MCP_ALLOWLIST` | `yicli.headless.mcp.allowlist` | 空 | 无头模式 MCP 白名单 |

其他常见 `-D` 属性：

| 属性 | 默认值 | 说明 |
|------|--------|------|
| `yicli.react.token.budget` | `Integer.MAX_VALUE` | ReAct token 硬预算 |
| `yicli.react.stagnation.window` | `3` | 相同调用停滞窗口 |
| `yicli.react.hard.max.iterations` | `50` | 硬轮数上限 |
| `yicli.llm.connect/read/write/call.timeout.seconds` | 60/300/60/600 | LLM 超时 |
| `yicli.history.size` / `yicli.history.fileSize` | 2000 / 10000 | 输入历史 |
| `yicli.task.workers` | 2 | 后台任务 worker |
| `yicli.render.columns` | 终端宽度 | Markdown 渲染列宽 |
| `yicli.render.color` | 自动 | 强制开启/关闭 ANSI 颜色 |

---

## 16. 面试可能追问的"为什么"

1. **为什么压缩阈值要留 20k + 13k？** 20k 给摘要输出预留空间，13k 是自动压缩触发的安全缓冲，避免触发压缩后 summary 写入就超窗。
2. **为什么 token 预算默认不限？** 长上下文模型（200k/1M）面向套餐用户，硬预算墙会打断长任务；软提示（80% window）+ 停滞窗口 + 轮数上限做兜底，CI 场景显式开启。
3. **为什么 read 超时 300s？** SSE 下 readTimeout 是两次 read 间隔，GLM-5.1 生成大段 reasoning 可能长时间静默。
4. **为什么流开始后不重试？** 避免已输出的内容重复/错位；重试只对"请求未开始"的失败生效。
5. **为什么并行工具默认 4？** 平衡吞吐与 LLM 上下文/终端输出稳定性，且保持结果原始顺序。
6. **为什么快照不写项目 `.git`？** Side-Git 快照库在 `~/.yicli/snapshots/`，避免污染用户仓库，`/restore <N>` 按 turn 恢复。
7. **为什么 PathGuard 是主防线而 CommandGuard 只是辅助？** 命令黑名单永远不完整，路径围栏 + HITL 审批才是可证明的安全边界。
